{
  "@context": "https://schema.org",
  "@type": "Article",
  "mainEntityOfPage": {
    "@type": "WebPage",
    "@id": "{{ url }}"
  },
  "headline": "{{ pageTitle }}",
  "description": "{{ pageDescription }}",
  "image": "{{ site.authorImage }}",
  "datePublished": "{{ page.date | 'date_to_xmlschema' }}",
  {% if page.data.lastModified %}
  "dateModified": "{{ page.data.lastModified | 'date_to_xmlschema' }}",
  {% else %}
  "dateModified": "{{ page.date | 'date_to_xmlschema' }}",
  {% endif %}
  "author": {
    "@type": "Person",
    "name": "Jeremias Menichelli"
  },
    "publisher": {
    "@type": "Organization",
    "name": "Jeremias Menichelli",
    "logo": {
      "@type": "ImageObject",
      "url": "{{ site.logo }}"
    }
  }
}
