---
layout: base
---

{% include 'nav' %}

{% include 'header' %}

<main id="main">
  <div class="container">
    {% if excerpt %}
      <p class="page__excerpt">
        {{ excerpt | markdownify }}
      </p>
    {% endif %}
    
    {% if external_url %}
      <p class="post__note">
        <a href="{{ external_url }}">Article originally published at {{ host }}</a>
      </p>
    {% endif %}

    {{ content }}
  </div>
</main>
{% include 'footer' %}
