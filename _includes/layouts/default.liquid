<!DOCTYPE html>
<html lang="en" class="no-js">
  {% include 'head' %}
  <body>
    <button class="dark--toggle" aria-label="Toggle dark mode"></button>
    {% include 'nav' %}
    {% include 'header' %}
    <main id="main" role="main">
      <div class="container main__container">
        {% if excerpt %}
          <p class="page__excerpt">
            {{ excerpt | markdownify }}
          </p>
        {% endif %}
        
        {% if external_url %}
          <p class="post__note">
            <a href="{{ external_url }}">Article originally published at {{ host }}</a>
          </p>
        {% endif %}

        {{ content }}
      </div>
    </main>
    {% include 'footer' %}
  </body>
</html>
